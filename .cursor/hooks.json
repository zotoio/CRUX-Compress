{
  "$schema": "https://cursor.sh/hooks-schema.json",
  "version": 1,
  "description": "CRUX compression hooks - detects source file changes and queues for compression",
  "hooks": {
    "sessionStart": [
      {
        "command": "bash .cursor/hooks/crux-session-start.sh",
        "description": "CRUX: Display pending CRUX compressions at session start"
      }
    ],
    "afterFileEdit": [
      {
        "command": "bash .cursor/hooks/crux-detect-changes.sh",
        "description": "CRUX: Queue modified source files (with crux: true) for CRUX compression"
      }
    ]
  }
}
