# Subtask: Config & Scaffolding

## Metadata
- **Subtask ID**: 01
- **Feature**: CRUX Memories
- **Assigned Subagent**: generalPurpose
- **Dependencies**: None
- **Created**: 20260403

## Objective

Create the foundational configuration and directory structure for the CRUX Memories system. This establishes the schema that all subsequent subtasks depend on.

## Deliverables Checklist
- [ ] `.crux/crux-memories.json` — full config file matching the schema in `docs/crux-memories.md` Section 2, with `unitOfWork` set to `"plan"` and `platform` set to `"cursor"`
- [ ] `memories/` directory tree with type subdirectories: `core/`, `redflag/`, `goal/`, `learning/`, `idea/`, `archived/`
- [ ] `memories/agents/` directory (empty, ready for agent-scoped memories)
- [ ] `.crux/reference-tracking/` directory (empty, ready for tracker files)
- [ ] `.gitkeep` files in empty directories to ensure they're tracked by git
- [ ] `.gitignore` additions for memory system generated files:
  - `.crux/memory-index.yml` — generated by index script, rebuilt on demand
  - `.crux/reference-tracking/` — generated tracker files, rebuilt lazily
  - `.ai-ignored/` — archive directory for executed plans and compression sources
- [ ] Config `flags.enableMemories` defaults to `"false"` (opt-in)
- [ ] Config `flags.enableMemoryCompression` defaults to `"false"`

## Definition of Done
- [ ] `.crux/crux-memories.json` is valid JSON and matches the spec schema
- [ ] All directories exist and are tracked by git
- [ ] No linter errors in any created files
- [ ] No linter errors in modified files

## Implementation Notes

Reference `docs/crux-memories.md` Section 2 "Configuration Schema" for the complete JSON structure. Key points:

- `storage.memoriesDir` = `"memories"`
- `storage.agentMemoriesDir` = `"memories/agents"`
- `storage.archiveDir` = `".ai-ignored/executed"`
- `storage.compressionSourceArchive` = `".ai-ignored/memories/sources"`
- `storage.indexFile` = `".crux/memory-index.yml"`
- `unitOfWork` = `"plan"`
- `commands.dream.file` = `".cursor/commands/crux-dream.md"`
- `commands.mindReader.file` = `".cursor/commands/crux-mindreader.md"`
- `hooks.sessionStartNudge.watchDir` = `"plans"` (matches `unitOfWork` = `"plan"`)
- All `typeTransitions` thresholds per spec
- `referenceTracking` config per spec
- `scopeRanking` and `scopes` per spec

Ensure the `platform` field is set to `"cursor"` since this is the primary platform.

## Testing Strategy
**IMPORTANT**: Do NOT trigger global test suites during parallel execution. Instead:
- Validate JSON syntax of config file
- Verify directory structure exists
- Defer full test suite execution to the final verification phase

## Execution Notes
[To be filled by executing agent]

### Agent Session Info
- Agent: [Not yet assigned]
- Started: [Not yet started]
- Completed: [Not yet completed]

### Work Log
[Agent adds notes here during execution]

### Blockers Encountered
[Any blockers or issues]

### Files Modified
[List of files changed]
