# Subtask: Update Config and Rules

## Metadata
- **Subtask ID**: 03
- **Feature**: crux-recall
- **Assigned Subagent**: crux-software-engineer
- **Dependencies**: None
- **Created**: 20260425

## Objective
Update configuration and rule files to rename all `mindreader`/`MindReader` references to `recall`/`Recall`. This includes the memory config JSON, the integration rule source, and regenerating the CRUX-compressed rule output.

## Deliverables Checklist
- [x] `.crux/crux-memories.json`: `commands.mindReader` key renamed to `commands.recall`, file path updated from `crux-mindreader.md` to `crux-recall.md`, default command updated
- [x] `.cursor/rules/crux-memories-integration.md`: All `/crux-mindreader` references updated to `/crux-recall`, all `mindreader`/`MindReader` updated to `recall`/`Recall`
- [x] `.cursor/rules/crux-memories-integration.crux.mdc`: Regenerated from updated source using the `crux-cursor-rule-manager` subagent

## Definition of Done
- [x] No remaining `mindreader` references in `.crux/crux-memories.json`
- [x] No remaining `mindreader` references in `.cursor/rules/crux-memories-integration.md`
- [x] `.cursor/rules/crux-memories-integration.crux.mdc` is regenerated and reflects the updated source
- [x] JSON file is valid (parseable)
- [x] No linter errors in modified files

## Implementation Notes
- For the JSON config: be careful with key renaming — the key itself changes from `mindReader` to `recall`, not just the values
- For the integration rule: this is the SOURCE file — edit this, then regenerate the `.crux.mdc` output
- To regenerate the CRUX compressed file, delegate to `crux-cursor-rule-manager` subagent as specified in the `_CRUX-RULE.mdc` workspace rule
- **CRITICAL**: Do NOT edit `.crux.mdc` files directly — always edit the source `.md` and regenerate

## Testing Strategy
**IMPORTANT**: Do NOT trigger global test suites during parallel execution. Instead:
- Validate JSON syntax of `.crux/crux-memories.json` (e.g., `python -c "import json; json.load(open(...))"`)
- Grep modified files for remaining `mindreader` references (case-insensitive)
- Verify `.crux.mdc` was regenerated (check `sourceChecksum` changed)

## Execution Notes

### Agent Session Info
- Agent: crux-software-engineer
- Started: 2026-04-25 21:52 AEST
- Completed: 2026-04-25 21:54 AEST

### Work Log
1. Read all three target files to understand current state
2. Renamed `mindReader` key to `recall` in `.crux/crux-memories.json`, updated file path and default command values
3. Updated `/crux-mindreader` to `/crux-recall` in `.cursor/rules/crux-memories-integration.md`
4. Delegated CRUX regeneration to `crux-cursor-rule-manager` subagent — sourceChecksum updated from `193025359` to `3144050868`
5. Validated JSON syntax (passes), confirmed zero remaining `mindreader` references in all three files

### Blockers Encountered
None

### Files Modified
- `.crux/crux-memories.json` — key rename `mindReader` → `recall`, file path and default command updated
- `.cursor/rules/crux-memories-integration.md` — `/crux-mindreader` → `/crux-recall`
- `.cursor/rules/crux-memories-integration.crux.mdc` — regenerated by `crux-cursor-rule-manager` (surgical diff + checksum update)

### Adversarial Verification (zoto-spec-judge)
- **Verified by**: zoto-spec-judge (independent)
- **Timestamp**: 2026-04-25T21:57+10:00
- **Verdict**: **Verified**
- `.crux/crux-memories.json`: Key confirmed as `"recall"` (lines 36–40), file path is `crux-recall.md`, default is `/crux-recall`. JSON validates cleanly via `python3 -c "import json; json.load(...)"`
- Case-insensitive grep for `mindreader` returned zero matches across all three files
- `.cursor/rules/crux-memories-integration.md`: Single change confirmed — `/crux-mindreader` → `/crux-recall` on line 18
- `.cursor/rules/crux-memories-integration.crux.mdc`: sourceChecksum updated from 193025359 → 3144050868. Independent `cksum` of source file outputs 3144050868 — **checksums match**. CRUX body updated `mindreader→recall` in amnesia section.
- No linter errors detected
